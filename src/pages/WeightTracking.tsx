import React, { useState } from 'react';\n\nconst WeightTracking = () => {\n    const [weight, setWeight] = useState('');\n    const [weightHistory, setWeightHistory] = useState([]);\n\n    const handleWeightChange = (e) => {\n        setWeight(e.target.value);\n    };\n\n    const handleSubmit = (e) => {\n        e.preventDefault();\n        if (weight) {\n            setWeightHistory([...weightHistory, weight]);\n            setWeight('');\n        }\n    };\n\n    const handleDelete = (index) => {\n        const newWeightHistory = weightHistory.filter((_, i) => i !== index);\n        setWeightHistory(newWeightHistory);\n    };\n\n    return (\n        <div>\n            <h2>Weight Tracking</h2>\n            <form onSubmit={handleSubmit}>\n                <input\n                    type="number"\n                    value={weight} \n                    onChange={handleWeightChange}\n                    placeholder="Enter weight"\n                    required\n                />\n                <button type="submit">Add</button>\n            </form>\n            <h3>Weight History</h3>\n            <ul>\n                {weightHistory.map((w, index) => (\n                    <li key={index}>\n                        {w} kg <button onClick={() => handleDelete(index)}>Delete</button>\n                    </li>\n                ))} \n            </ul>\n            {/* Chart display can go here */}\n        </div>\n    );\n};\n\nexport default WeightTracking;